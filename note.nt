// Using built-in-state concept
1) useState function: this a react hook that allows us to define value as state where changes to these values should reflect

whenever state is updated and depends on the previous state we should use alternative form Usinga function in setinput that will return.

//////////
Two way binding allows us to gather inputs in form and also set it to the value and also to make clearing after entered poddible

//////////////
The following can be used for Conditional content in JSX:
1) Using ternary operation
2) using && (if first condition is true) will return the last part of the operation 
3) Setting variable and using if statement depending on the variable 

///////////
Avoiding using same class name or id twice
1) Using a package: styled components
2) CSS modules

//////////
Using react developer tools: Extensuin f0r debugging

/////////////
working with fragment, can be used in 3 ways 
1) React.fragment
2) Fragmant but wil import fragment from react
3) Using empty element tag (<> </>)

/////////
Working with portals, for positioning the interface codes like overlay not being the same element tag with normal content display

///////
Working with useRef instead of useState
will return an object with key of current and value properties as value


///////////////////////////
Advance handling using
1) useEffect
2) useReducer
3) useContext

///////////////////////////////
1) Using useImperativeHandler: Allows variables in the same component to reused unlike props.
2) React.forward: to make ref value available on other component mostly for focusing on inputs
3) React.memo: allows optimization of components to avoid re-rendering of child component in parent component but those works for object so useCallback have to used to allow that.

////////////////////////////////////////
class-based components 

//////////////////////////////////////
using try catch in sending http request along side using useEffect and useCallback.

//////////////////////////
useCallback can be used on useEffect to stop infinte loop


//////////////////////////////////
BUILDING CUSTOM REACT hook

it allows other hooks to be call inside it function and it function name must start wit ( use ) e.g useCounter.